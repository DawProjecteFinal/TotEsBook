<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE glassfish-web-app PUBLIC "-//GlassFish.org//DTD GlassFish Application Server 3.1 Servlet 3.0//EN" "http://glassfish.org/dtds/glassfish-web-app_3_0-1.dtd">
<glassfish-web-app error-url="">
  
  <!-- Desactivem CDI implícit per evitar conflictes amb Spring -->
  <property name="disable-implicit-cdi" value="true"/>
  
  <!-- Mapeig del nom JNDI del DataSource configurat a persistence.xml 
       amb el nom JNDI real configurat a GlassFish (si s'escau). 
       Si no fas servir JTA/DataSource de GlassFish, aquesta part no és necessària.
  <resource-ref>
      <res-ref-name>jdbc/TotEsBookDS</res-ref-name> 
      <jndi-name>jdbc/__TimerPool</jndi-name> <!- Nom JNDI real a GlassFish ->
  </resource-ref>
  -->
  
  <class-loader delegate="false"/> <!-- Important posar delegate="false" per prioritzar les llibreries del WAR -->
  <jsp-config>
    <property name="keepgenerated" value="true">
      <description>Keep a copy of the generated servlet class' java code.</description>
    </property>
  </jsp-config>
</glassfish-web-app>